import * as Styled from './styles'

export const Header = () => {
  return (
    <Styled.HeaderContainer>
      <Styled.LogoText>Paulo</Styled.LogoText>
      <i>menuIcon</i>
    </Styled.HeaderContainer>
  )
}
